# Log: [Change Name]

<!--
FALLBACK TEMPLATE — use only when Backlog.md is unavailable. With Backlog.md,
the milestone task's Implementation Notes are the session journal and the
`waiting` label replaces the Waiting On table (see references/backlog-md.md).

The artifacts are the only memory between sessions. Write every entry so
that someone with zero context — tomorrow's you, another agent, your human
partner — can resume cold. Append entries at the TOP (newest first).
Never end a session without an entry, even mid-task.
-->

## Waiting On

| Item | Who owes it | Requested | Follow up on | Status |
|------|-------------|-----------|--------------|--------|
| [e.g. signed approval of draft] | [name/team] | YYYY-MM-DD | YYYY-MM-DD | ⏳ open / ✅ received YYYY-MM-DD |

## Sessions

### YYYY-MM-DD

- **Done:** [what actually got completed — reference task numbers]
- **Decisions:** [what was decided and why — especially anything that changed brief/spec/tasks]
- **In progress:** [any half-finished task and exactly where it stands]
- **Next action:** [the single concrete first step for the next session — startable cold, e.g. "draft section 3 using numbers in research/costs.md", not "continue report"]

### YYYY-MM-DD

- **Done:** [...]
- **Next action:** [...]
